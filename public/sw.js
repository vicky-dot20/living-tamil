const CACHE="living-tamil-v2";const DISCOVERIES=["/","/offline","/kural","/discover/01-kurinji","/discover/02-mullai","/discover/03-marutham","/discover/04-neithal","/discover/05-palai","/discover/06-five-worlds","/discover/07-your-landscape","/discover/11-kural-beginning","/discover/12-aram","/discover/13-yaathum-oore","/discover/21-korkai","/discover/22-puhar","/discover/23-evidence"];
self.addEventListener("install",event=>{event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll(DISCOVERIES)).then(()=>self.skipWaiting()))});self.addEventListener("activate",event=>{event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(key=>key!==CACHE).map(key=>caches.delete(key)))).then(()=>self.clients.claim()))});self.addEventListener("fetch",event=>{const request=event.request;if(request.method!=="GET")return;const url=new URL(request.url);if(url.origin!==location.origin||url.pathname.startsWith("/api/"))return;if(request.mode==="navigate"){event.respondWith(fetch(request).then(response=>{const copy=response.clone();caches.open(CACHE).then(cache=>cache.put(request,copy));return response}).catch(()=>caches.match(request).then(cached=>cached||caches.match("/offline"))));return}if(url.pathname.startsWith("/_next/static/")){event.respondWith(caches.match(request).then(cached=>cached||fetch(request).then(response=>{const copy=response.clone();caches.open(CACHE).then(cache=>cache.put(request,copy));return response})))}});
