import fs from"node:fs";import path from"node:path";const root=process.cwd();const requiredMeta=["title","tamilTitle","journey","journeyTitle","order","category","summary","sourceTitle","sourceUrl","license","reviewStatus"];const requiredSections=["Hook","Original","Simple","English","Context","Today"];const failures=[];for(const file of fs.readdirSync(path.join(root,"content","discoveries")).filter(name=>name.endsWith(".md"))){const text=fs.readFileSync(path.join(root,"content","discoveries",file),"utf8");const [,meta="",body=""]=text.split(/^---\s*$/m);for(const key of requiredMeta)if(!new RegExp(`^${key}:\\s*.+$`,`m`).test(meta))failures.push(`${file}: missing ${key}`);for(const section of requiredSections)if(!new RegExp(`^## ${section}\\s*$`,`m`).test(body))failures.push(`${file}: missing ${section} section`);const status=meta.match(/^reviewStatus:\s*(.+)$/m)?.[1]?.trim();if(status==="reviewed")for(const key of["reviewer","reviewDate","reviewScope"])if(!new RegExp(`^${key}:\\s*.+$`,`m`).test(meta))failures.push(`${file}: reviewed content requires ${key}`)}if(failures.length){console.error(failures.join("\n"));process.exit(1)}console.log("Content validation passed.");
