const TEXT_BASE="https://thirukkural.senkanthal.org/kural"; const MEANING_BASE="https://tamil-kural-api.vercel.app/api/kural";
type TextData={id:number;kural:string}; type MeaningData={chapter:string;section:string;number:number;meaning:{ta_mu_va:string;ta_salamon:string;ta_kalaignar:string;en:string}};
export async function GET(_request:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params;const number=Number(id);if(!Number.isInteger(number)||number<1||number>1330)return Response.json({error:"Invalid Kural number."},{status:400});try{const options={next:{revalidate:604800}} as const;const[textResponse,meaningResponse]=await Promise.all([fetch(`${TEXT_BASE}/${number}`,options),fetch(`${MEANING_BASE}/${number}`,options)]);if(!textResponse.ok||!meaningResponse.ok)throw new Error();const text=await textResponse.json() as TextData;const meaning=await meaningResponse.json() as MeaningData;return Response.json({number,lines:text.kural.split("$"),chapter:meaning.chapter,section:meaning.section,tamil:[{author:"மு. வரதராசனார்",text:meaning.meaning.ta_mu_va.replace(/^மு\.வ\s*:\s*/,"")},{author:"சாலமன் பாப்பையா",text:meaning.meaning.ta_salamon.replace(/^சாலமன் பாப்பையா\s*:\s*/,"")},{author:"கலைஞர்",text:meaning.meaning.ta_kalaignar.replace(/^கலைஞர்\s*:\s*/,"")}],english:meaning.meaning.en,sources:{text:"Senkanthal Thirukkural API",meaning:"nramc/thirukkural-api"},license:"Provisional reuse status; attribution retained"},{headers:{"Cache-Control":"public, s-maxage=604800, stale-while-revalidate=2592000"}})}catch{return Response.json({error:"Kural details are temporarily unavailable."},{status:503})}}
